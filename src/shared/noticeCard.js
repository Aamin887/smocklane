import Button from "./button"

const NoticeCards = ({}) => {
    return(
        <div>
            <h3>{}</h3>
            <h1>{}</h1>

            <p>{}</p>

            <Button text={''} onClick={''}/>
        </div>
    )
}

export default NoticeCards;